{
  "name": "yawnlessai",
  "version": "0.3.0",
  "description": "Yawnless.ai CLI — orchestrate AI agent teams to run a business",
  "type": "module",
  "bin": {
    "yawnlessai": "./dist/index.js"
  },
  "keywords": [
    "yawnless",
    "ai",
    "agents",
    "orchestration",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yawnlessai/yawnless.git",
    "directory": "cli"
  },
  "homepage": "https://github.com/yawnlessai/yawnless",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node --input-type=module -e \"import esbuild from 'esbuild'; import config from './esbuild.config.mjs'; await esbuild.build(config);\" && chmod +x dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@yawnlessai/adapter-claude-local": "workspace:*",
    "@yawnlessai/adapter-codex-local": "workspace:*",
    "@yawnlessai/adapter-cursor-local": "workspace:*",
    "@yawnlessai/adapter-opencode-local": "workspace:*",
    "@yawnlessai/adapter-pi-local": "workspace:*",
    "@yawnlessai/adapter-openclaw-gateway": "workspace:*",
    "@yawnlessai/adapter-utils": "workspace:*",
    "@yawnlessai/db": "workspace:*",
    "@yawnlessai/server": "workspace:*",
    "@yawnlessai/shared": "workspace:*",
    "drizzle-orm": "0.38.4",
    "dotenv": "^17.0.1",
    "commander": "^13.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
